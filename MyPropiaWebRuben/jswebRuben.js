const app = Vue.createApp({
    data() {
        return {
            mensaje: '¡Hola, mundo desde Vue!'
            
        };
    }
    // //////////////////////////////////////////////////////////////////////////////fin de data
});

app.mount('#app');